<div class="thumbnail">
	<a href="{{ path_for('product.get', {slug: product.slug}) }}"><img src="{{ product.image }}" alt="{{ produtc.title }} image"></a>
	<div class="caption">
		<h4><a href="{{ path_for('product.get', {slug: product.slug}) }}">{{ product.title }}</a></h4>
			<p>{{ product.discription }}</p>
	</div>
</div>
